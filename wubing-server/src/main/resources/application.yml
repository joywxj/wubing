server:
  port: 9999

eureka:
  client:
    registerWithEureka: false #是否将自己的信息注册到Eureka 服务器
    fetchRegistry: false #是否从eureka服务 器中抓取注册信息
    register-with-eureka: false
    fetch-registry: false
    serviceUrl:
      defaultZone: http://localhost:9999/eureka/
# this is new coinfig ,I will push it